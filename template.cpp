#pragma gcc optimizer   ("O3")
#pragma gcc target      ("sse4")
#include <bits/stdc++.h>
using namespace std;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
	const int N = int(5);
	const int reset = N*4;
	int h[N];
	int t,n;
    cin>>t;
    while(t--){
        cin>>n;
		for(int i=0;i<n;++i){
	
		}
		memset(h,0,reset);
    }
}
